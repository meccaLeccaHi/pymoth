#!/usr/bin/env python3

def main():

    print('Testing params module:')

    from show_figs import show_FA_thumbs
    # thumb_array = np.zeros((self._len_side, self._num_per_class, self._class_num))
    # _thumb_array[self._active_pixel_inds,:,:] = digit_queues
    # normalize = 1
    # n_thumbnails = 1
    # screen_size = (1920, 1080)
    # show_FA_thumbs(_thumb_array, n_thumbnails, normalize, 'Input thumbnails',
    #     self.SCREEN_SIZE, self.RESULTS_FOLDER + _os.sep + 'thumbnails')

    # show_FA_thumbs(_thumb_array, 1, 1, 'Input thumbnails', (1920,1080), 'foo/thumbnails')
    # print('\tshow_FA_thumbs method test passed')
