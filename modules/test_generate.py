#!/usr/bin/env python3

from generate import extract_mnist_feature_array, classify_digits_log_likelihood, classify_digits_thresholding

# create dummy data

# test extract_mnist_feature_array
# extract_mnist_feature_array( mnist, labels, image_indices, phase_label )

# test cropDownsampleVectorizeImageStack
# cropDownsampleVectorizeImageStack( im_stack, crop_val, ds_ratio, ds_method )

# test averageImageStack
# averageImageStack( im_stack, indices_to_average )
